version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
  pre_build:
    commands:
      - chmod +x ./gradlew
  build:
    commands:
      - ./gradlew clean build --no-daemon

artifacts:
  files:
    - appspec.yml
    - scripts/**/*
    - build/libs/*.jar
  discard-paths: no
